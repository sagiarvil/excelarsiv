---
import CommerceLayout from '../layouts/CommerceLayout.astro';
import HeroSection from '../components/home/HeroSection.astro';
import { getAllTemplates, getTemplateSearchIndex } from '../lib/templates';
import { kararPages } from '../data/kararPages';

const templates = await getAllTemplates();
const searchIndex = await getTemplateSearchIndex();
const productCount = templates.length;
const categoryCount = new Set(templates.map((item) => item.categorySlug)).size;
const minPrice = Math.min(...templates.map((item) => item.priceTL));
const priceLevels = [...new Set(templates.map((item) => item.priceTL))].sort((a, b) => a - b);
const featured = [
  '13-haftalik-nakit-akisi-ve-odeme-planlama-sistemi',
  'aylik-patron-finans-paneli',
  'banka-kredi-ve-taksit-takip-sistemi',
  'cari-hesap-tahsilat-ve-musteri-risk-takip-sistemi',
].map((slug) => templates.find((item) => item.slug === slug)).filter(Boolean);
const homeDecisionSlugs = [
  'kobi-nakit-akisi-excel',
  'mali-musavir-cari-takip-excel',
  'pos-komisyon-kontrol-excel',
  'trendyol-pazaryeri-net-kar-excel',
];
const homeDecisions = homeDecisionSlugs.map((slug) => kararPages.find((item) => item.slug === slug)).filter(Boolean);
const faqItems = [
  { q: 'Satın almadan önce dosyayı inceleyebilir miyim?', a: 'Uygun ürünlerde ücretsiz demo ve gerçek ürün ekranları bulunur. Ürün detay sayfasından kapsamı, fiyatı ve demo bağlantısını satın almadan önce kontrol edebilirsiniz.' },
  { q: 'Fiyatlar ana sayfada elle mi yazılıyor?', a: 'Hayır. Bu sayfadaki ürün sayısı ve örnek ürün fiyatları mevcut ürün veri kaynağından build sırasında hesaplanır.' },
  { q: 'Ödeme nerede yapılıyor?', a: 'Ödeme Shopier üzerinde tamamlanır. Excel Arşiv kart bilgisi saklamaz; ödeme sonrasında teslimat akışı üzerinden ürün dosyasına erişilir.' },
  { q: 'Hangi sistemi seçmem gerektiğini bilmiyorsam ne yapmalıyım?', a: 'Karar rehberlerinden işletme probleminize en yakın soruyu seçebilir veya ürün bulucuyu kullanabilirsiniz.' },
  { q: 'Excel Arşiv bir ERP veya muhasebe programı mı?', a: 'Hayır. Ürünler belirli finans, muhasebe ve operasyon karar problemlerine odaklanan indirilebilir Excel çalışma sistemleridir.' },
];
const homeJsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    { '@type': 'WebSite', '@id': 'https://excelarsiv.com/#website', name: 'Excel Arşiv', url: 'https://excelarsiv.com/', inLanguage: 'tr-TR' },
    { '@type': 'FAQPage', '@id': 'https://excelarsiv.com/#faq', mainEntity: faqItems.map((item) => ({ '@type': 'Question', name: item.q, acceptedAnswer: { '@type': 'Answer', text: item.a } })) },
  ],
};
---

<CommerceLayout
  title="İşletmeler İçin Hazır Excel Karar Sistemleri | Excel Arşiv"
  description="Nakit akışı, kredi, maliyet, kârlılık ve muhasebe süreçleri için hazır Excel karar sistemleri. Gerçek ekranları ve demoları görün; ihtiyacınıza uygun sistemi seçin."
>
  <Fragment slot="head"><script type="application/ld+json" set:html={JSON.stringify(homeJsonLd)} /></Fragment>

  <div class="home-v2">
    <HeroSection searchIndex={searchIndex} />

    <section class="catalog-proof" data-experience-stage>
      <div class="home-shell">
        <div class="section-head">
          <div><p class="eyebrow">EN ÇOK TERCİH EDİLEN AMİRAL SİSTEMLER</p><h2>Önce işletmenizin para ve kontrol problemini çözen sistemi seçin.</h2></div>
          <a href="/sablonlar">Tüm 51+ sistemi gör →</a>
        </div>
        <div class="price-levels">{priceLevels.map((price) => <span>{price.toLocaleString('tr-TR')} TL</span>)}</div>
        <div class="featured-grid featured-grid--amiral">
          {featured.map((item) => item && (
            <article class="featured-card">
              <a class="shot" href={item.url} aria-label={`${item.name} sistem detayını incele`}><img class="premium-card__shot" src={item.kapak} alt={`${item.name} ürün kapağı`} width="1200" height="900" loading="lazy" /></a>
              <div class="featured-card__body">
                <small>{item.categoryName}</small>
                <h3><a href={item.url}>{item.name}</a></h3>
                <p>{item.summary}</p>
                <div class="featured-card__bottom">
                  <strong>{item.priceTL.toLocaleString('tr-TR')} TL</strong>
                  <div class="featured-card__actions">
                    <a href={item.url} class="btn-inspect">İncele →</a>
                  </div>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="difference" data-experience-stage>
      <div class="home-shell difference-shell">
        <div class="difference-head">
          <p class="eyebrow">ALTYAPI VE GÜVENLİK STANDARTLARI</p>
          <h2>Yazılım kurmak, aylık lisans ödemek veya güvenlik riski almak yok.</h2>
          <p class="difference-desc">Tüm sistemler uluslararası kurumsal standartlarda, sıfır makro güvenliği ve açık formül şeffaflığıyla hazırlanır.</p>
        </div>
        <div class="difference-cards">
          <div class="diff-card">
            <span class="diff-card__icon" aria-hidden="true">🛡️</span>
            <div>
              <strong>Sıfır Makro &amp; Güvenlik</strong>
              <p>Virüs veya trojan riski taşıyan VBA kodu içermez; saf .xlsx formatında güvenle çalışır.</p>
            </div>
          </div>
          <div class="diff-card">
            <span class="diff-card__icon" aria-hidden="true">🔓</span>
            <div>
              <strong>%100 Açık Formül Şeffaflığı</strong>
              <p>Kapalı veya şifreli hücre yoktur; tüm formül mimarisi açık, incelenebilir ve denetlenebilirdir.</p>
            </div>
          </div>
          <div class="diff-card">
            <span class="diff-card__icon" aria-hidden="true">💻</span>
            <div>
              <strong>Tüm Cihazlarla Uyum</strong>
              <p>Windows Excel, Mac Excel, iPad ve Microsoft 365 ile tam uyum; harici eklenti gerekmez.</p>
            </div>
          </div>
          <div class="diff-card">
            <span class="diff-card__icon" aria-hidden="true">♾️</span>
            <div>
              <strong>Aylık Lisans / Abonelik Yok</strong>
              <p>Bir kez satın alın, ömür boyu sınırsız kullanın. Kullanıcı başı ücret veya yenileme maliyeti sıfırdır.</p>
            </div>
          </div>
          <div class="diff-card">
            <span class="diff-card__icon" aria-hidden="true">⏱️</span>
            <div>
              <strong>10 Saniyede Teslim</strong>
              <p>Shopier 256-Bit SSL güvencesiyle ödeyin; indirme bağlantısı anında e-postanıza ulaşsın.</p>
            </div>
          </div>
          <div class="diff-card">
            <span class="diff-card__icon" aria-hidden="true">🏛️</span>
            <div>
              <strong>17 Yıllık Bankacılık Güvencesi</strong>
              <p>Ticari bankacılık ve saha finans deneyimiyle, gerçek şirket verileriyle test edilmiş karar modelleri.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 1- SİZE ÖZEL (Dengeli, simetrik, 4 adımlı zengin iş akışı & CTA) -->
    <section class="custom-build" data-experience-stage>
      <div class="home-shell custom-build-grid">
        <div class="custom-build-copy">
          <p class="eyebrow">SIZE ÖZEL</p>
          <h2>Hazır sistem yetmiyorsa, çalışma işletmenizin işleyişine göre kurulur.</h2>
          <p class="custom-lead">Muhasebe ofisleri ve işletmeler için mevcut Excel düzeni, veri akışı, kontrol noktaları ve raporlama ihtiyacı analiz edilerek özel çalışma hazırlanır.</p>
          <div class="custom-trust-chips">
            <span>✓ 17 Yıllık Finans Tecrübesi</span>
            <span>✓ Gizlilik Sözleşmesi</span>
            <span>✓ Birebir Kurulum</span>
          </div>
        </div>
        <div class="custom-actions">
          <div class="custom-steps-grid">
            <div class="custom-step">
              <span class="step-num">01</span>
              <div>
                <strong>İşleyişi Anlatın</strong>
                <p>Mevcut dosyanızı veya çalışma biçiminizi paylaşın.</p>
              </div>
            </div>
            <div class="custom-step">
              <span class="step-num">02</span>
              <div>
                <strong>Kapsamı Netleştirelim</strong>
                <p>Giriş, hesap ve karar ekranlarını birlikte tanımlayalım.</p>
              </div>
            </div>
            <div class="custom-step">
              <span class="step-num">03</span>
              <div>
                <strong>Sisteminizi Kuralım</strong>
                <p>İş kurallarınıza özel hatasız Excel mimarisi tasarlansın.</p>
              </div>
            </div>
            <div class="custom-step">
              <span class="step-num">04</span>
              <div>
                <strong>Doğrudan Test & Teslimat</strong>
                <p>Gerçek verileriniz üzerinden doğrulanıp teslim edilsin.</p>
              </div>
            </div>
          </div>
          <div class="custom-cta-card">
            <div class="custom-cta-text">
              <span class="custom-trust-pill">15 DAKİKA ÖN TEŞHİS</span>
              <p>Derdinizi teknik dile çevirmeden, doğrudan finansal ihtiyacınızı konuşalım.</p>
            </div>
            <a class="custom-cta-btn" href="/ozel-excel-sistemleri">Özel Sistemleri İnceleyin →</a>
          </div>
        </div>
      </div>
    </section>

    <!-- 2- KARAR YARDIMI (Dikkat çeken, interaktif ve yüksek etkili karar kartları) -->
    <section class="decision-help" data-experience-stage>
      <div class="home-shell decision-layout">
        <div class="decision-intro">
          <p class="eyebrow">KARAR YARDIMI</p>
          <h2>Hangi sistemi alacağınızı bilmiyorsanız, sorununuzu seçin.</h2>
          <p>İşletmenizin yaşadığı darboğaza en yakın problemden başlayın. Doğru ürünü belirlemek için karar rehberlerini karşılaştırın.</p>
          <a class="decision-all-btn" href="/karar">18 karar rehberinin tamamı →</a>
        </div>
        <div class="decision-grid">
          {homeDecisions.map((page, index) => page && (
            <a href={`/karar/${page.slug}`} class="decision-card">
              <div class="decision-card-head">
                <span class="decision-no">0{index + 1}</span>
                <span class="decision-tag">{index === 0 ? 'Nakit Akışı' : index === 1 ? 'Cari & Tahsilat' : index === 2 ? 'POS & Banka' : 'E-Ticaret & Kâr'}</span>
              </div>
              <div class="decision-card-body">
                <h3>{page.title}</h3>
                <p>{page.description}</p>
                <span class="decision-link">Kararı inceleyin →</span>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>

    <section class="authority" data-experience-stage>
      <div class="home-shell authority-panel">
        <div class="authority-copy">
          <p class="eyebrow light">TASARLAYAN VE DENETLEYEN</p>
          <h2>Barış Bağırlar</h2>
          <p class="authority-lead">17 yıllık ticari bankacılık ve saha finans yöneticiliği tecrübesiyle; Excel Arşiv sistemleri yalnız tablo düzenlemek için değil, yönetim kurulu ve şirket sahiplerine net karar aldırmak için hazırlanır.</p>
          <div class="authority-links">
            <a href="/hakkinda">Uzman profili →</a>
            <a href="/neden-excel-arsiv">Yöntem ve farklar →</a>
          </div>
        </div>
        <div class="authority-metrics">
          <article><small>KATALOG</small><strong>{productCount}</strong><span>satışta sistem</span></article>
          <article><small>KAPSAM</small><strong>{categoryCount}</strong><span>işletme alanı</span></article>
          <article><small>TECRÜBE</small><strong>17+</strong><span>yıl finans & banka</span></article>
        </div>
      </div>
    </section>

    <section class="faq-close" data-experience-stage>
      <div class="home-shell faq-grid">
        <div><p class="eyebrow">SATIN ALMADAN ÖNCE</p><h2>Sık sorulan sorular</h2><p>Ürünü seçmeden önce kapsam, demo ve teslimat bilgilerini kontrol edin.</p><div class="close-actions"><a class="primary" href="/sablonlar">Sistemleri inceleyin</a><a href="/urun-bulucu">Ürün bulucu</a></div></div>
        <div class="faq-list">{faqItems.map((item) => <details><summary>{item.q}</summary><p>{item.a}</p></details>)}</div>
      </div>
    </section>
  </div>
</CommerceLayout>

<style>
  :root{--font-sans:ui-sans-serif,system-ui,sans-serif;--font-mono-face:ui-monospace,SFMono-Regular,Menlo,monospace}
  [data-experience-stage]{content-visibility:auto;contain-intrinsic-size:auto 760px;scroll-margin-top:92px}
  .home-v2{--green:#166534;--green-dark:#14532d;--ink:#162019;--muted:#647068;--line:#dfe5e0;--soft:#f6f8f6;background:#fff;color:var(--ink)}
  .home-shell{width:min(1180px,calc(100% - 48px));margin:0 auto}
  .eyebrow{margin:0;font:800 10px/1.2 var(--font-mono);letter-spacing:.14em;color:var(--green)}
  .eyebrow.light{color:#8ce9b1}
  .catalog-proof,.custom-build,.decision-help,.faq-close{padding:64px 0;border-bottom:1px solid var(--line)}
  .section-head{display:flex;justify-content:space-between;gap:24px;align-items:end;margin-bottom:28px}
  .section-head h2,.custom-build h2,.decision-intro h2,.authority h2,.faq-close h2,.difference h2{margin:10px 0 0;font-size:clamp(30px,4vw,48px);line-height:1.04;letter-spacing:-.04em}
  .section-head>a,.decision-intro>a{color:var(--green-dark);font-weight:800}
  .featured-grid--amiral{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:16px;align-items:stretch}
  .featured-card{display:flex;min-width:0;height:100%;flex-direction:column;border:1px solid #dfe6e1;border-radius:18px;overflow:hidden;background:#fff;box-shadow:0 8px 24px rgba(15,23,42,.04);transition:transform .18s ease,box-shadow .18s ease,border-color .18s ease}
  .featured-card:hover{transform:translateY(-4px);border-color:#059669;box-shadow:0 16px 36px rgba(5,150,105,.1)}
  .shot{display:block;aspect-ratio:16/10;overflow:hidden;border-bottom:1px solid #dfe6e1;background:#f8faf9}
  .shot img{width:100%;height:100%;object-fit:cover}
  .featured-card__body{display:flex;flex:1;flex-direction:column;padding:20px}
  .featured-card__body small{font:800 10px/1 var(--font-mono);color:#059669;letter-spacing:.08em;text-transform:uppercase}
  .featured-card__body h3{margin:10px 0 8px;font-size:18px;line-height:1.2;letter-spacing:-.02em}
  .featured-card__body h3 a{color:var(--ink);text-decoration:none}
  .featured-card__body h3 a:hover{color:#059669}
  .featured-card__body p{min-height:54px;font-size:12.5px;line-height:1.6;color:var(--muted);margin:0}
  .featured-card__bottom{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:auto;padding-top:16px;border-top:1px solid #eef2ef}
  .featured-card__bottom strong{font-size:18px;font-weight:850;color:#0f172a}
  .btn-inspect{display:inline-flex;align-items:center;padding:8px 14px;border-radius:10px;background:#f0fdf4;border:1px solid #bbf7d0;color:#087a46;font-size:12.5px;font-weight:750;text-decoration:none;transition:all .15s ease}
  .btn-inspect:hover{background:#059669;border-color:#059669;color:#fff}
  @media(max-width:1180px){.featured-grid--amiral{grid-template-columns:repeat(2,minmax(0,1fr))}}
  @media(max-width:640px){.featured-grid--amiral{grid-template-columns:1fr}}
  /* ALTYAPI VE GÜVENLİK STANDARTLARI (PDF Sayfa 5 Bento) */
  .difference{padding:74px 0;background:linear-gradient(180deg,#fff 0%,#f8faf9 100%);border-bottom:1px solid var(--line);color:var(--ink)}
  .difference-shell{display:grid;gap:36px}
  .difference-head{max-width:760px}
  .difference-desc{color:var(--muted);line-height:1.7;font-size:15px;margin-top:12px}
  .difference-cards{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}
  .diff-card{display:flex;align-items:flex-start;gap:14px;padding:22px 20px;border:1px solid #dfe6e1;border-radius:18px;background:#fff;box-shadow:0 8px 24px rgba(15,23,42,.04);transition:transform .18s ease,border-color .18s ease,box-shadow .18s ease}
  .diff-card:hover{transform:translateY(-3px);border-color:#059669;box-shadow:0 14px 32px rgba(5,150,105,.08)}
  .diff-card__icon{font-size:22px;line-height:1;padding:10px;border-radius:12px;background:#eef7f2;flex-shrink:0}
  .diff-card strong{display:block;font-size:15px;font-weight:800;color:#0f172a;line-height:1.25}
  .diff-card p{margin:6px 0 0;font-size:13px;line-height:1.55;color:#526176}
  @media(max-width:980px){.difference-cards{grid-template-columns:repeat(2,minmax(0,1fr))}}
  @media(max-width:640px){.difference-cards{grid-template-columns:1fr}}
  
  /* SIZE ÖZEL (Custom Build) */
  .custom-build{background:#fff}
  .custom-build-grid{display:grid;grid-template-columns:1.05fr .95fr;gap:48px;align-items:start}
  .custom-lead{max-width:560px;color:var(--muted);line-height:1.75;font-size:15px}
  .custom-trust-chips{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px}
  .custom-trust-chips span{padding:6px 12px;border-radius:999px;background:#f0fdf4;border:1px solid #bbf7d0;color:#15803d;font:700 11px/1 var(--font-mono)}
  .custom-actions{display:flex;flex-direction:column;gap:10px}
  .custom-steps-grid{display:flex;flex-direction:column;gap:9px}
  .custom-step{display:grid;grid-template-columns:38px 1fr;gap:14px;padding:14px 16px;border:1px solid var(--line);border-radius:14px;background:var(--soft);transition:all .2s ease}
  .custom-step:hover{background:#fff;border-color:var(--green);box-shadow:0 4px 14px rgba(22,101,52,.08);transform:translateX(3px)}
  .step-num{display:flex;align-items:center;justify-content:center;width:34px;height:34px;border-radius:10px;background:rgba(22,101,52,.1);color:var(--green);font:800 11px/1 var(--font-mono)}
  .custom-step strong{font-size:15px;color:var(--ink);display:block}
  .custom-step p{margin:3px 0 0;color:var(--muted);line-height:1.5;font-size:12px}
  .custom-cta-card{margin-top:4px;padding:16px 18px;border-radius:16px;background:linear-gradient(135deg,#10251a 0%,#173b28 100%);color:#fff;display:flex;flex-wrap:wrap;align-items:center;justify-content:space-between;gap:12px;box-shadow:0 8px 24px rgba(16,37,26,.15)}
  .custom-trust-pill{font:800 9.5px/1 var(--font-mono);letter-spacing:.06em;color:#8ce9b1;text-transform:uppercase}
  .custom-cta-text p{margin:4px 0 0;font-size:11.5px;color:#c4d0c8}
  .custom-cta-btn{display:inline-flex;align-items:center;justify-content:center;padding:10px 16px;background:#8ce9b1;color:#0b2214;font-weight:800;font-size:12px;border-radius:10px;text-decoration:none;transition:all .2s ease;white-space:nowrap}
  .custom-cta-btn:hover{background:#fff;transform:translateY(-1px);box-shadow:0 4px 12px rgba(255,255,255,.2)}

  /* KARAR YARDIMI (Decision Help) */
  .decision-help{background:linear-gradient(180deg,#f7faf8 0%,#edf3ef 100%);padding:56px 0}
  .decision-layout{display:grid;grid-template-columns:.75fr 1.25fr;gap:48px;align-items:center}
  .decision-intro p{color:var(--muted);line-height:1.65;font-size:14.5px;margin-top:10px}
  .decision-all-btn{display:inline-flex;align-items:center;margin-top:18px;padding:11px 20px;background:var(--green);color:#fff;font-weight:800;font-size:12.5px;border-radius:12px;text-decoration:none;box-shadow:0 4px 14px rgba(22,101,52,.18);transition:all .2s ease}
  .decision-all-btn:hover{background:var(--green-dark);transform:translateY(-2px);box-shadow:0 6px 20px rgba(22,101,52,.26)}
  
  .decision-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}
  .decision-card{display:flex;flex-direction:column;justify-content:space-between;padding:18px 18px;color:var(--ink);text-decoration:none;border:1px solid #d2ded5;border-radius:16px;background:#fff;box-shadow:0 2px 6px rgba(18,32,25,.03);transition:all .22s cubic-bezier(0.4,0,0.2,1)}
  .decision-card:hover{border-color:var(--green);background:#fdfefe;box-shadow:0 10px 24px rgba(22,101,52,.11);transform:translateY(-2px)}
  .decision-card-head{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:10px}
  .decision-no{display:inline-flex;align-items:center;justify-content:center;width:26px;height:26px;border-radius:8px;background:rgba(22,101,52,.08);color:var(--green);font:800 10.5px/1 var(--font-mono)}
  .decision-tag{font:800 9px/1 var(--font-mono);letter-spacing:.06em;text-transform:uppercase;color:var(--green-dark);background:#eaf3ed;border:1px solid rgba(22,101,52,.15);padding:4px 8px;border-radius:999px}
  .decision-card h3{margin:0;font-size:16px;line-height:1.24;font-weight:800;color:var(--ink)}
  .decision-card p{margin:6px 0 10px;color:var(--muted);font-size:11.5px;line-height:1.5}
  .decision-link{font-size:11.5px;color:var(--green);font-weight:800;display:inline-flex;align-items:center;gap:3px;transition:gap .2s ease}
  .decision-card:hover .decision-link{color:var(--green-dark);gap:6px}

  /* OTORİTE & METRİKLER */
  .authority{padding:0;background:#fff;border-bottom:1px solid var(--line)}
  .authority-panel{display:grid;grid-template-columns:1.1fr .9fr;background:#10251a;color:#fff}
  .authority-copy{padding:48px 52px}
  .authority-copy h2{color:#fff}
  .authority-lead{max-width:650px;color:#c4d0c8;line-height:1.75;font-size:14.5px;margin-top:12px}
  .authority-links{display:flex;gap:18px;flex-wrap:wrap;margin-top:22px}
  .authority-links a{color:#a7efc1;font-weight:800;font-size:13px}
  .authority-metrics{display:grid;grid-template-columns:repeat(3,1fr);border-left:1px solid #294236}
  .authority-metrics article{display:flex;flex-direction:column;justify-content:flex-end;min-height:240px;padding:28px;border-right:1px solid #294236}
  .authority-metrics article:last-child{border-right:0}
  .authority-metrics small{font:800 9px/1 var(--font-mono);letter-spacing:.12em;color:#87d9a6}
  .authority-metrics strong{font-size:44px;line-height:1;margin:10px 0 6px}
  .authority-metrics span{font-size:11px;color:#aebdb4}

  /* SSS */
  .faq-grid{display:grid;grid-template-columns:.8fr 1.2fr;gap:58px;align-items:start}
  .faq-close>div>div>p{color:var(--muted);line-height:1.7}
  .close-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:24px}
  .close-actions a{padding:12px 18px;border:1px solid #b9c5bc;color:var(--ink);font-weight:800;text-decoration:none;border-radius:12px}
  .close-actions .primary{background:var(--green);border-color:var(--green);color:#fff}
  .faq-list{border-top:1px solid var(--line)}
  .faq-list details{padding:17px 0;border-bottom:1px solid var(--line)}
  .faq-list summary{font-weight:800;cursor:pointer}
  .faq-list p{margin:10px 0 0;color:var(--muted);line-height:1.65}
  
  @media(max-width:900px){
    .featured-grid{grid-template-columns:1fr}
    .custom-build-grid,.decision-layout,.authority-panel,.faq-grid,.difference-grid{grid-template-columns:1fr}
    .authority-metrics{border-left:0;border-top:1px solid #294236}
    .authority-copy{padding:38px 30px}
    .section-head{align-items:start;flex-direction:column}
  }

  @media(max-width:620px){
    .home-shell{width:min(100% - 24px,1180px)}
    .catalog-proof,.custom-build,.faq-close{padding:40px 0}
    .decision-help{padding:36px 0}
    .decision-grid{grid-template-columns:1fr}
    .difference{padding:44px 0}
    .difference-cards{grid-template-columns:1fr;gap:9px}
    .diff-card{padding:13px 14px;border-radius:13px}
    .custom-step{grid-template-columns:32px 1fr;padding:12px 14px;gap:11px}
    .step-num{width:30px;height:30px;font-size:10px}
    .authority-metrics{grid-template-columns:repeat(3,1fr)}
    .authority-metrics article{min-height:110px;padding:14px 8px;text-align:center;border-right:1px solid #294236;border-bottom:none}
    .authority-metrics article:last-child{border-right:0}
    .authority-metrics strong{font-size:26px}
    .authority-copy{padding:30px 20px}
    .featured-grid h3{font-size:19px}
  }
</style>